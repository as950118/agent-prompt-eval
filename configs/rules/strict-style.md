# Strict style rules
- Follow PEP 8 for Python code.
- Use type hints where appropriate.
- Prefer explicit over implicit; avoid magic.
- Keep functions small and single-purpose.
- Add a one-line docstring for non-trivial functions.
